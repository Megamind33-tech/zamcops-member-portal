# The ZAMCOPS Member Portal prototype runs entirely on client-side mock data
# and requires no environment variables or API keys to run.
#
# APP_URL: Optional. The URL where this app is hosted. Used for self-referential
# links if/when a backend is added.
# APP_URL="http://localhost:3000"
